# ミッションのあらまし

あなたの目標は、以下の「問題」セクションに書かれた問題を解けるLeanプログラムを書き、
さらに、その正当性をLean内で形式的に証明することです。

プログラムは、このファイルと同じディレクトリにある`Main.lean`に書いてください。
それ以外のファイルは編集してはいけません。

`Main.lean`内に必ず書く必要があるのは、以下の3つです:

- `solve`関数
  - 「問題」セクションで指定された入力データを引数として受け取る。
  - あなたの決めたデータ型の、出力データを返す。
- `printSolution`関数
  - あなたの決めたデータ型の、出力データを引数として受け取る。
  - 返り値の型は`IO Unit`。
  - IOアクションとして、渡された出力データを、`IO.println`などを用いて標準出力に出力する。
- 定理`solution_is_valid`
  - `solve`関数の返り値が、問題で指定された条件を実際に満たしていることを示す定理。

`Main.lean`内には、必要と判断した範囲で、上記以外の関数や定理を含めてかまいません。
また、`lakefile.lean`では`mathlib`が依存先として登録されているので、
`Main.lean`内ではこれをインポートしてもかまいません。


# 問題

## 入力データ

- 自然数(`Nat`)N

## 制約

- Nは20以下である。

## 問題文

長さ N の正しいカッコ列をすべて、辞書順に出力してください。

ただし、正しいカッコ列は次のように定義されています :

- () は正しいカッコ列である
- S が正しいカッコ列であるとき、文字列 ( + S + ) は正しいカッコ列である
- S,T が正しいカッコ列であるとき、文字列 S + T は正しいカッコ列である
- それ以外の文字列はすべて、正しいカッコ列でない

例えば、

"()()"
"(()())(())"
"()()()()()()()()"

は正しいカッコ列ですが、

")("
")))()((("
"((((a))))"

は正しいカッコ列ではありません。

また、 ( の方が ) よりも辞書順で早いものとします。

## 出力

長さ N の正しいカッコ列をすべて、辞書順に、改行区切りで出力してください。
